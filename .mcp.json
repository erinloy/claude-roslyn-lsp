{
  "mcpServers": {
    "roslyn-refactor": {
      "command": "${LOCALAPPDATA}\\Programs\\PowerShell\\7\\pwsh.exe",
      "args": ["-NoProfile", "-ExecutionPolicy", "Bypass", "-File", "${CLAUDE_PLUGIN_ROOT}/boot/run.ps1", "mcp"],
      "env": {}
    }
  }
}
