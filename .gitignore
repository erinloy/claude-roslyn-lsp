# .NET build output (global — covers every current and future project, incl. extensions-api/ and samples/)
**/bin/
**/obj/

# Runtime data (built bridge, downloaded Roslyn server, logs) — produced on device, never committed
.data/

# OS / editor
.DS_Store
Thumbs.db
*.swp
